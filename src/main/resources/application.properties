

#数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/yao
spring.datasource.username=root
spring.datasource.password=yang1234
spring.jpa.database=MYSQL
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# The SQL dialect makes Hibernate generate better SQL for the chosen database
# Hibernate 原语
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect ？？啥意思

# Hibernate ddl auto (create, create-drop, validate, update)
# DDL 级别 (create, create-drop, validate, update)
#spring.jpa.hibernate.ddl-auto = update

spring.jpa.hibernate.ddl-auto=update
logging.level.org.hibernate.SQL=DEBUG
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE